<UserControl x:Class="Parking.App.Views.Components.TicketListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Parking.App.Views.Components"
             mc:Ignorable="d" 
              >
    <UserControl.Resources>
        <DataTemplate x:Key="carPlateItemTemplate">
            <Border x:Name="border" CornerRadius="7" Background="#33828FA8" Margin="0" Padding="10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border Padding="5" >
                            <TextBlock Text="{Binding LicensePlate}" FontStyle="Normal" FontWeight="Bold" FontSize="20"/>
                        </Border>
                        <Border x:Name="seizedBox" Visibility="Collapsed" Padding="5" VerticalAlignment="Center" Background="Red" CornerRadius="8">
                            <TextBlock Text="توقیفی" FontSize="10"/>
                        </Border>
                    </StackPanel>

                    <TextBlock FontSize="14" TextWrapping="Wrap" HorizontalAlignment="Center">
                            <Run Text="{Binding VehicleSegmentName}"/>
                            <LineBreak/>
                            <Run Text="{Binding StartTimeString}"/>
                            <Run Text=" | "/>
                            <Run Text="{Binding StartTimeOnlyString}" FontSize="18"/>
                            <!--<LineBreak/>
                            <Run Text="{Binding BarcodeId}"/>-->
                    </TextBlock>
                </StackPanel>
            </Border>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsSeized}" Value="true">
                    <Setter TargetName="border" Property="Background" Value="#50FF0000"/>
                    <Setter TargetName="seizedBox" Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>
    <ScrollViewer VerticalAlignment="Stretch">
        <ui:ListView x:Name="TicketList"
              VirtualizingStackPanel.IsVirtualizing="True" 
              VirtualizingStackPanel.VirtualizationMode="Recycling"
              ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
              SelectionMode="Single"
              SelectionChanged="TicketList_Selected"
              ItemTemplate="{StaticResource carPlateItemTemplate}"/>
    </ScrollViewer>
</UserControl>
