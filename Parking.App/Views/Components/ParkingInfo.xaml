<UserControl x:Class="Parking.App.Views.Components.ParkingInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Parking.App.Views.Components"
             mc:Ignorable="d" FontFamily="{StaticResource IranYekanFont}" 
             d:DesignHeight="50" d:DesignWidth="800"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"

             >
    <Grid>
        <Border CornerRadius="6" Padding="0 5"  Background="#20B9B9B9"  Margin="0 5 5 0">
            <StackPanel  x:Name="TopStatusBar" >
                <DockPanel  FlowDirection="RightToLeft">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="20 0 20 0">
                        <Image HorizontalAlignment="Center" Margin="5 0" Source="/Assets/Madaar-Logo.png" Height="30" />
                        <ui:SymbolIcon x:Name="ServerStatusIcon" Symbol="CloudCheckmark16" 
                                       Foreground="{DynamicResource SystemFillColorSuccessBrush}" ToolTip="ارتباط با سرور برقرار میباشد"/>
                        <ui:SymbolIcon x:Name="DbStatusIcon" Margin="10 0 0 0"
                   Symbol="Database16"
                   Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                   ToolTip="ارتباط با پایگاه داده برقرار نیست"/>


                    </StackPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        
                        <TextBlock x:Name="PakingNameText"     Margin="15 0 0 0" Text="........"  HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <Separator  Margin="5 0"/>
                        <ui:SymbolIcon Symbol="Person16" Margin="3 0"/>
                        <TextBlock Text="کاربر: " VerticalAlignment="Center" />
                        <TextBlock x:Name="UserFullName" Text="نام کاربر" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Center" FlowDirection="LeftToRight">
                        <TextBlock x:Name="DateText" Text="12 آذر 1403"  HorizontalAlignment="Right"  VerticalAlignment="Center"/>
                        <ui:SymbolIcon Symbol="CalendarLtr16"/>

                        <TextBlock x:Name="TimeText" Width="60" Margin="15 0 0 0"  Text="22:30:50"  HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <ui:SymbolIcon Symbol="Clock16"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
