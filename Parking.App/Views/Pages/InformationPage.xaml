<Page x:Class="Parking.App.Views.Pages.InformationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Parking.App.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      FontFamily="{StaticResource IranYekanFont}"  
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="InformationPage">

    <StackPanel FlowDirection="RightToLeft" Margin="10">
        <TextBlock Text="اطلاعات" Grid.Row="0"  FontWeight="Bold" FontSize="15"    Margin="10"  />
        <Border Background="#20B9B9B9" Margin="5" Padding="10" CornerRadius="7">
            <StackPanel FlowDirection="RightToLeft" HorizontalAlignment="Stretch">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Margin="5"  Text="{Binding ParkingName}" VerticalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Left" Margin="5"  Text="نام" VerticalAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
        <Border Background="#20B9B9B9" Margin="5" Padding="10" CornerRadius="7">
            <StackPanel FlowDirection="RightToLeft" HorizontalAlignment="Stretch">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Margin="5"  Text="{Binding TotalCards}" VerticalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Left" Margin="5"  Text="تعداد کارت های تعریف شده" VerticalAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
        <Border Background="#20B9B9B9" Margin="5" Padding="10" CornerRadius="7">
            <StackPanel FlowDirection="RightToLeft" HorizontalAlignment="Stretch">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Margin="5" Text="{Binding TotalDiscountedCards}" VerticalAlignment="Center"/>
                    <Button DockPanel.Dock="Left" Margin="5" VerticalAlignment="Center" 
                    Background="Transparent" BorderThickness="0" Foreground="Blue" 
                    Cursor="Hand" Click="CardWithDisCount_LinkButton_Click">
                        <TextBlock Text="تعداد کارت های تخفیف دار تعریف شده" TextDecorations="Underline"/>
                    </Button>

                </DockPanel>
            </StackPanel>
        </Border>

        <Border Background="#20B9B9B9" Margin="5" Padding="10" CornerRadius="7">
            <StackPanel FlowDirection="RightToLeft" HorizontalAlignment="Stretch">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Margin="5"  Text="{Binding TotalSpaces}" VerticalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Left" Margin="5"  Text="فضای تعریف شده" VerticalAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
        <Border Background="#20B9B9B9" Margin="5" Padding="10" CornerRadius="7">
            <StackPanel FlowDirection="RightToLeft" HorizontalAlignment="Stretch">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Margin="5"  Text="{Binding OccupiedSpaces}" VerticalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Left" Margin="5"  Text="فضای پر شده" VerticalAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
        <Border Background="#20B9B9B9" Margin="5" Padding="10" CornerRadius="7">
            <StackPanel FlowDirection="RightToLeft" HorizontalAlignment="Stretch">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Margin="5"  Text="{Binding AvailableSpaces}" VerticalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Left" Margin="5"  Text="تعداد فضای خالی" VerticalAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
        
    </StackPanel>
</Page>
