<Page x:Class="Parking.App.Views.Pages.RolePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:Parking.App.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:components="clr-namespace:Parking.App.Views.Components"
      xmlns:helpers="clr-namespace:Parking.App.Helpers"
      
      FontFamily="{StaticResource IranYekanFont}"  
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      mc:Ignorable="d"
      Title="RolePage" d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel FlowDirection="RightToLeft" Margin="10">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="لیست نقش‌ها" FontWeight="Bold" FontSize="15" Margin="10 0"/>
            <ui:Button helpers:PermissionBehavior.Permission="AddRole" Content="افزودن نقش" Click="AddRole_Click" Margin="10 0"/>
        </StackPanel>

        <ui:DataGrid x:Name="RolesDataGrid"  
                     Margin="0 10" 
                     FlowDirection="RightToLeft" 
                     AutoGenerateColumns="False" 
                     IsReadOnly="True" 
                     ItemsSource="{Binding Items, Mode=OneWay}"
                     CanUserAddRows="False">
            <ui:DataGrid.Columns>
                <DataGridTextColumn Header="نام نقش" Binding="{Binding Name, Mode=OneWay}" />
                <DataGridTextColumn Header="نام فارسی" Binding="{Binding FaName, Mode=OneWay}" />
                <DataGridTemplateColumn Header="عملیات">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <ui:Button Content="مدیریت دسترسی"
                                           helpers:PermissionBehavior.Permission="ManagePermission"
                                   Command="{Binding DataContext.ManagePermissionCommand,
                                             RelativeSource={RelativeSource AncestorType=Page}}"
                                   CommandParameter="{Binding}" 
                                   Margin="5 0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </StackPanel>
</Page>
