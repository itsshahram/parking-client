<Page x:Class="Parking.App.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Parking.App.Views.Pages"
      mc:Ignorable="d" 
      FontFamily="{StaticResource IranYekanFont}"  
      d:DesignHeight="450" d:DesignWidth="800"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="SettingsPage">

    <Grid>
        <Grid FlowDirection="RightToLeft" Margin="7">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="تنظیمات" Grid.Row="0"  FontWeight="Bold" FontSize="15"    Margin="10 5"  />
            <ui:NavigationView
       x:Name="RootNavigation"
       Grid.Row="2"
       Padding="3"
       Margin="2"
       FooterMenuItemsSource="{Binding ViewModel.FooterMenuItems, Mode=OneWay}"
       FrameMargin="5"
       IsBackButtonVisible="Collapsed"
       IsPaneToggleVisible="True" 
       MenuItemsSource="{Binding ViewModel.MenuItems, Mode=OneWay}" OpenPaneLength="200"
       PaneDisplayMode="LeftMinimal" >

                <ui:NavigationView.ContentOverlay>
                    <Grid>
                        <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
                    </Grid>
                </ui:NavigationView.ContentOverlay>
            </ui:NavigationView>

            <ContentPresenter
       x:Name="RootContentDialog"
       Grid.Row="1"
       Grid.RowSpan="2" />
        </Grid>
    </Grid>
</Page>
